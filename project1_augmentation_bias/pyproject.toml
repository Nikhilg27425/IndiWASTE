[project]
name = "indiwaste-project1-augmentation-bias"
version = "0.1.0"
description = "Project 1 for IndiWASTE: augmentation robustness comparison on clean and corrupted test sets"
readme = "program.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26",
    "pandas>=2.3",
    "pillow>=10.0",
    "scikit-learn>=1.5",
    "torch>=2.4",
    "torchvision>=0.19",
]

[tool.uv]
package = false
