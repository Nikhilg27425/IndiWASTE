[project]
name = "indiwaste-project2-attention-surgery"
version = "0.1.0"
description = "Project 2 for IndiWASTE: systematic attention-head ablation study on pretrained ViT-B/16"
readme = "program.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26",
    "pandas>=2.3",
    "pillow>=10.0",
    "scikit-learn>=1.5",
    "torch>=2.4",
    "torchvision>=0.19",
]

[tool.uv]
package = false
